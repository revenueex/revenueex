/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RevenueRealizedComponent } from './revenue-realized.component';

describe('Component: RevenueRealized', () => {
  it('should create an instance', () => {
    let component = new RevenueRealizedComponent();
    expect(component).toBeTruthy();
  });
});
